# QEMU USB Hub 单层测试配置
args = [
  "-nographic",
  "-cpu", "cortex-a53",
  "-usb",

  # xHCI 控制器
  "-device", "qemu-xhci,id=xhci",

  # Level 1 Hub
  "-device", "usb-hub,bus=xhci.0,id=hub1",

  # 测试设备（自动连接到 Hub 端口）
  "-device", "usb-kbd",
  "-device", "usb-mouse",
]

fail_regex = []
success_regex = []
to_bin = true
uefi = false
